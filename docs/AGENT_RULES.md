# Agent Rules

All agents must follow these rules:

- Read PROJECT_SPEC.md before starting
- Do not modify files outside assigned ownership
- Maintain MVVM architecture
- Use Kotlin + Jetpack Compose only
- Use ExoPlayer for playback
- Keep modules loosely coupled
- Avoid unnecessary dependencies
- Write production-style clean code
- Prefer readability over complexity
- Explain reasoning before implementation
- Never overwrite another agent's work without approval